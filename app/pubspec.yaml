name: aibank_app
description: AIBank mobile app with GenUI
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  a2a: ^4.2.0
  flutter:
    sdk: flutter
  genui: ^0.7.0
  genui_a2ui: ^0.7.0
  http: ^1.2.2
  json_schema_builder: ^0.1.3
  logging: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
